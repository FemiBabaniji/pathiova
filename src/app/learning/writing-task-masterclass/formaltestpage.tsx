"use client";  // Ensure this component runs on the client-side

import MCQWritingTaskUN from '@/components/MCQWritingTaskUN'; // Verify this path

const MCQPage1 = () => {
  return <MCQWritingTaskUN />;
};

export default MCQPage1;
